<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gloss.chatroom.mapper.MessageMapper">
    <select id="getLastMessageBySessionId" resultType="java.lang.String">
        select content from message where sessionId = #{sessionId} order by postTime desc limit 1;
    </select>
    <select id="getMessagesBySessionId" resultType="com.gloss.chatroom.model.Message">
        select messageId, sessionId, user.username as fromName, fromId, content from message
            join user on message.fromId = user.id and message.sessionId = #{sessionId}
            order by postTime desc limit 100;
    </select>
    <insert id="addMessage">
        insert into message(sessionId, fromId, content)
        values(#{sessionId}, #{fromId}, #{content});
    </insert>
</mapper>